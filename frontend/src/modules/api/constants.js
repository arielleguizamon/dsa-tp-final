export const REDUCER_KEY = "api";

const prefix = "API/";

const ActionTypes = {
  REQUEST: prefix + "REQUEST",
  SUCCESS: prefix + "SUCCESS",
  FAILURE: prefix + "FAILURE"
};

export default ActionTypes;
